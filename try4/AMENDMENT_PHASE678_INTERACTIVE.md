# Try-4 Phase 6–8 interactive execution amendment

The user authorized the current interactive Codex agent to develop and execute
Phases 6, 7 and 8 continuously instead of using the slower external Codex CLI.
The agent may consume the allowed structured-review inputs listed in `try4.md`,
including GT reference renders and deterministic discrepancies, but the repair
executor receives no GT STEP/B-Rep, exact GT sketches, GT feature tree or copied
dimensions.

Context isolation is false. This is a development execution on DEV_A/B and must
not be presented as a blind or confirmatory benchmark. The two-part pilot may be
used to debug and freeze the repair method. After that freeze, all 12 parts remain
in the formal denominator and no favorable-case dropping or threshold tuning is
allowed. Robot C remains untouched.
