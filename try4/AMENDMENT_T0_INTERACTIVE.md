# Try-4 Phase 3 amendment: current interactive Codex execution

Approved by the user during Phase 3 on 2026-09-06 because external Codex CLI
latency was excessive. Three CLI outputs are retained under the ignored
`artifacts/T0_CLI_partial/` directory; R01_P03 was interrupted and R01_P04 was
not started. Four pre-generation CLI implementation incidents are preserved in
`results/t0_execution_incidents.csv`. These records are not selected or reused
as the current T0 condition.

The active Phase-3 condition is `T0_INTERACTIVE`: the current Codex conversation
authors exactly one direct Feature Graph/CAD IR artifact for each of the five
DEV_A Macro-Parts. A deterministic materializer adds only schema boilerplate and
does not choose geometry. The same generic Plan Gate and FreeCAD executor apply
to all five Parts. No review, repair, T1/T2 skill-guided generation or transfer
execution occurs in this phase.

This change removes the independent ephemeral packet-only guarantee. The current
conversation has inspected offline source annotations, reference contact sheets,
the repository and prior Try-3/CLI artifacts. T0_INTERACTIVE is therefore a
development baseline only, not a controlled isolated-context or confirmatory
model experiment. Exact per-Part context tokens, sampling controls, model
snapshot and latency are unavailable. Later T0/T1/T2 causal comparison requires
a newly frozen, consistent execution mode and new eligible evaluation cases.

