Predict an anonymous RMDG v1 JSON from the Image+Text input. Infer rigid links, topology, joints, axes and coarse geometry only from the evidence. Use IDs L0, L1, ... and J0, J1, ...; never use product or hidden URDF names. Keep every interface and functional-role string under 80 characters; use `unknown` instead of explanatory prose. Return JSON only matching the supplied RMDG v1 schema.
