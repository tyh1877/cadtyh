# Try-5A.2 Phase 1–3 requirement evidence

- [x] D0 provenance is frozen as a direct reference to Try-5A.1 C1; no body or interface parameter is regenerated.
- [x] The attachment evaluator measures B-Rep relations between each physical interface carrier and that link's BODY geometry.
- [x] The virtual-link filter classifies L11 as `virtual_frame` and excludes it from physical geometry, rendering and collision populations.
- [x] D0 is re-evaluated with separate logical-interface and physical-attachment metrics.
- [x] Exact C1 collision raw results are copied as an immutable D0 evaluator reference.
- [x] Stop before Phase 4–8: no repair scheduler, D1 or D2 artifact exists.
