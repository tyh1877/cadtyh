# Try-5B.1-A2a three-link formal development comparison

Evidence is required for each item before a cross-link claim is made.

- [ ] Freeze the L04 audited method prompts by SHA-256; substitute only Link ID and raw Link-specific evidence.
- [ ] Freeze qwen3.7-plus, seed, temperature, top-p, one call, zero refinement rounds, output ceiling, timeout, SDK, and zero manual intervention.
- [ ] Freeze three pilot Links, the development-only 96 configurations, relevant joint sweeps, F0 CAD, interfaces, scaffold policy, and evaluator hashes.
- [ ] Confirm the 32-case formal holdout lock remains `accessed=false` and `evaluation_count=0` before and after execution.
- [ ] Save six expanded requests; prove each Link pair has identical shared text and attachments, with only the method prompt differing.
- [ ] Save all six request IDs, returned model identifiers, full responses, token usage, latency, and any infrastructure failures.
- [ ] Perform one model generation per Link × condition; do not retry model-output, CAD, or evaluator failures.
- [ ] Prove Structured response → fresh IR → compiler dispatch → CAD for each successful run; retain failures without fallback.
- [ ] Prove Direct response → exact generated code → FreeCAD body → CAD for each successful run; retain failures without editing generated code.
- [ ] Apply the same frozen scaffold and protected-interface policy, FreeCAD runtime, canonical pose, and evaluators in both methods.
- [ ] Preserve all 96 development configurations in every mechanical denominator; record failures explicitly.
- [ ] Save geometry, assembly, motion, collision, process, per-link delta, mean delta, median delta, and a six-run main table without an overall score.
- [ ] Independently audit model/input/scaffold/call/evaluator/prompt parity, GT isolation, manual intervention, and retry accounting.
- [ ] Report observed failure modes by Link, cross-link pattern, FACTS, INTERPRETATION, NOT_SUPPORTED, and bounded Try-6 design implications.
- [ ] Stop after the three-link development comparison; do not evaluate formal holdout or implement Try-6.
