# Try-6.0-R1-v3 requirement-to-evidence checklist

Final representation-hardening round. C1-v1, R0, R1-v1 and R1-v2 are frozen.
No GT, formal holdout cases, C1-v2, C2 or KFDE.

- [ ] Audit prior result hashes and holdout lock; freeze three visual slots, authority sources, unchanged Parameter Registry and prompt before API calls.
- [ ] Derive the API transport schema from one canonical slot schema; prove local slot/evidence rules remain stricter.
- [ ] Pass slot completeness, unknown/duplicate ID, enum, evidence, status-dependent assembly, parameter mapping, relations, authority, no-repair and holdout-lock tests.
- [ ] Make exactly one production-schema compatibility preflight; stop if API rejects the schema or raw transport output is invalid.
- [ ] Only after preflight PASS, freeze all N=5 inputs, model/budget and implementation hashes.
- [ ] Retain five fresh same-input calls; independently compute transport/contract/assembly/KFDG gates and per-slot perception stability without voting.
- [ ] Only if representation gate is 5/5, use one **fresh** non-denominator VLM call and 2–5 candidate no-GT E2E smoke.
- [ ] If E2E runs, prove URDF 63 mm anchor→objective→theta→CAD, interface invariance, connected solid, export/reopen.
- [ ] Independently validate final decision, all-case failure accounting, prior-result immutability, GT=0 and holdout lock; stop before C1-v2.
